/* YOUR CODE GOES HERE */

/*
(function pollForTeamNav() {
  let $teamNavItem = $('a[href="#team"]');
  if (!$teamNavItem.length) return setTimeout(pollForTeamNav, 50);
  $('a[href="#team"]').text('Crew');
})();

$(document).on('click', '.intro-text a', function() {
  console.log('click');
  window.optimizely.push({
    type: 'event',
    eventName: 'hero-cta-click'
  });
});
*/

/*
(function pollForCTA() {
  let $heroCTA = $('.intro-text a');
  if (!$heroCTA.length) return setTimeout(pollForCTA, 50);
  $heroCTA.prop('href', '#contact');
  $('a[href="#team"]').text('Crew');
})();
*/

/*
(function pollForHeroImage() {
  let $header = $('header');
  if (!$header.length) return setTimeout(pollForHeroImage, 50);
  $header.css('background-image', 'url(../img/new-hero.jpg)');
})();
*/
